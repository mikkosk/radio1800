/*
import { Voice } from "../types";

const currentList: Voice[];

const createNewList = (length: number) => {
    const num = length;
    emptyList();

}

const emptyList = () => {
    currentList.length = 0;
}

const addVoice = () => {
    currentList.push();
}
*/